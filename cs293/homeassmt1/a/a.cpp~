#include <string>
#include <bitset>
#include <iostream>
using namespace std;

int main()
{
   string txt("hello");
   for(int i = 0; i < txt.length(); i++)
   {
      cout << bitset<8>(txt[i]);
   }
   return 0;
};
